Continue from handoff. Before doing anything else: (1) If state/intent_surface.md exists, read it first (intent, scope, constraints). (2) If state/session_brief.md exists, read it; it may override or supplement handoff. (3) Read handoff_latest.md (typically .cursor/state/handoff_latest.md). (4) If handoff has plan_ref or dependency_links, load those when Next requires it. (5) If Context (optional) is present, use it first; only load linked plans or state docs when Next requires details. Only after loading context, do the **Next** section; do not act on Next until you have read the handoff. If you have concerns, uncertainty, or pushback about the Next or scope, surface them first. Then update the handoff file with the next step. Respect the scope in handoff if present; do not drift outside it without asking. If handoff has intent, scope, or human_gate: respect them before acting. If handoff has Musts, Must-nots, or Escalation triggers: respect them. If Next says "Awaiting approval", ask human to approve before proceeding. Do not assume any context from a previous chat—the handoff file is the source of truth. When stuck: escalate after 3 attempts rather than looping. If handoff is empty or you're unsure what to do: ask the human.
